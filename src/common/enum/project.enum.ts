export enum ProjectStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  ARCHIVED = 'archived',
}
export enum ProjectRole {
  OWNER = 'owner',
  MANAGER = 'manager',
  MEMBER = 'member',
  VIEWER = 'viewer',
}
